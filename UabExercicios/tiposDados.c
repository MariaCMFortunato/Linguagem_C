#include <stdio.h>
#include <limits.h> 
int main(){

    printf("\nSizeof(unsigned int): %llu", sizeof(unsigned int));
    printf("\nunsigned int MinValue: 0");
    printf("\nunsigned int MaxValue: %u" , UINT_MAX);

}